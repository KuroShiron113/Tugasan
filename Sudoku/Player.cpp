#include "player.h"

void Player::makeMove(int &row, int &col, int &num) {
    // Implement player's move logic to get row, col, and num
    std::cout << "Enter row, column, and number (1-9): ";
    std::cin >> row >> col >> num;
}
